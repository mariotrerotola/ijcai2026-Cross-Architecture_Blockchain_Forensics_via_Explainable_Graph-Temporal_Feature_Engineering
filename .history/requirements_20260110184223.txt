#
# Project dependencies (Bitcoin + Ethereum pipelines)
# --------------------------------------------------
# Notes:
# - Some notebooks use optional accelerators (e.g., pyarrow for faster CSV reads).
# - Playwright requires an extra post-install step:
#     playwright install
#

# Core data stack
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Visualization
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18

# ML / Imbalanced learning
scikit-learn>=1.2
imbalanced-learn>=0.11
joblib>=1.3
cleanlab>=2.6

# Deep learning (used in Bitcoin_Fraud_Detection/mlp + distillation notebooks)
tensorflow>=2.13

# Explainability
shap>=0.44
ciu>=0.2

# Graph features (Ethereum feature extraction)
networkx>=3.1

# Utilities
requests>=2.31
psutil>=5.9
tqdm>=4.66
nest-asyncio>=1.6

# Web scraping (ChainAbuse fetcher)
beautifulsoup4>=4.12
playwright>=1.41

# Rule extraction (DEXiRE)
dexire>=0.1

# Optional: faster CSV engine (used with fallback in notebooks)
pyarrow>=14.0
